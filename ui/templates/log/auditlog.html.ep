% layout 'default', title => 'Лог аудита', back_url => '/clients';
<div class="scont">
  <table class="striped narrow-log">
    <thead>
      <tr>
	<th style="width:4%;">ID</th>
	<th style="width:11%;">ВРЕМЯ</th>
	<th>ПОЛЬЗОВАТЕЛЬ</th>
	<th>ДЕЙСТВИЕ</th>
      </tr>
    </thead>
    <tbody>
      % for (@{$log_rec->{d}}) {
      <tr>
	<td style="width:4%;"><%= $_->{id} %></td>
	<td style="width:11%;white-space:nowrap;"><%= $_->{date} %></td>
	<td><%= oprs $_->{login} %></td>
	<td><%= $_->{info} %></td>
      </tr>
      % }
    </tbody>
  </table>
  %= m_page_nav($log_rec->{page}, $log_rec->{pages}, {round=>3, outer=>1, start=>1, class=>'center-align', param=>'p', query=>'#top'});
  <p>Общее число записей: <%= $log_rec->{lines_total} %></p>
</div>


