% layout 'default', title => 'Лог аудита', back_url => '/';
<div class="scont">
  <table class="striped narrow-log">
    <thead>
      <tr>
	<th>ID</th>
	<th>ВРЕМЯ</th>
	<th>ПОЛЬЗОВАТЕЛЬ</th>
	<th>ДЕЙСТВИЕ</th>
      </tr>
    </thead>
    <tbody>
      % for (@{$log_rec->{d}}) {
      <tr>
	<td><%= $_->{id} %></td>
	<td><%= $_->{date} %></td>
	<td><%= $_->{login} %></td>
	<td><%= $_->{info} %></td>
      </tr>
      % }
    </tbody>
  </table>
  %= m_page_nav($log_rec->{page}, $log_rec->{pages}, {round=>3, outer=>1, start=>1, class=>'center-align', param=>'p', query=>'#top'});
  <p>Общее число записей: <%= $log_rec->{lines_total} %></p>
</div>


